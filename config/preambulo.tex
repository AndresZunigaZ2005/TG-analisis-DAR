% =====================================================================
%  PREÁMBULO — Resumen / Análisis DAR
%  Paquetes, colores, estilo de tablas y leyendas.
% =====================================================================

% ---------- Codificación e idioma -----------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
% Si tu distribución tiene el idioma instalado (Overleaf / TeX Live full),
% descomenta la línea siguiente para partición silábica en español:
% \usepackage[spanish,es-nodecimaldot]{babel}

% ---------- Geometría de página --------------------------------------
\usepackage[a4paper,margin=2.5cm]{geometry}

% ---------- Matemáticas -----------------------------------------------
\usepackage{amsmath}      % align, cases, \text, etc.

% ---------- Tablas ----------------------------------------------------
\usepackage{array}
\usepackage{makecell}
\usepackage[table]{xcolor}
\usepackage{ragged2e}
\usepackage{float}        % habilita el especificador [H] (tabla inquebrable)
\usepackage{caption}

% ---------- Listas ----------------------------------------------------
\usepackage{enumitem}     % permite continuar la numeración entre subsecciones

% ---------- Gráficos y enlaces ---------------------------------------
\usepackage{graphicx}
\usepackage{url}
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=blue,
  pdftitle={Resumen de la caracterización de las Tecnologías Proxy-Caché
            y Almacenamiento de Archivos},
  pdfauthor={Santiago Quintero Uribe, Juan Manuel Perdomo Cárdenas,
             Andrés Felipe Zúñiga Zuluaga}
}

% ---------- Colores institucionales / de tabla ------------------------
\definecolor{gridhead}{HTML}{C5E0B3}   % verde claro de encabezado
\definecolor{gridrow}{HTML}{EFEFEF}    % gris suave de filas alternas
\definecolor{verdeuq}{HTML}{009B3A}    % verde Universidad del Quindío

% =====================================================================
%  LEYENDAS (captions) DEBAJO DE LA TABLA
% =====================================================================
\captionsetup[table]{
  name=Tabla,
  labelfont=bf,
  labelsep=period,
  position=bottom,      % el paquete caption calcula el espaciado hacia abajo
  skip=6pt,
  font=small,
  justification=centering,
  singlelinecheck=true
}

% =====================================================================
%  APARIENCIA DE LAS TABLAS
% =====================================================================
\renewcommand{\arraystretch}{1.25}
\setlength{\tabcolsep}{2.5pt}
\setlength{\intextsep}{12pt plus 3pt minus 3pt}

% Evita que LaTeX estire el texto cuando una tabla se empuja de página
\raggedbottom

% =====================================================================
%  ESPACIADO DE PÁRRAFOS
% =====================================================================
\setlength{\parskip}{0.4em}
\setlength{\parindent}{0pt}

% ---------- Cabeceras de tabla (paquete makecell) ---------------------
\renewcommand{\theadfont}{\bfseries}
\renewcommand{\theadalign}{cc}

% Margen de tolerancia para líneas largas sin puntos de división silábica
\setlength{\emergencystretch}{3em}
